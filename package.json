{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "release/index.js",
  "scripts": {
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node release/index.js",
    "start:debug": "./node_modules/.bin/ts-node --inspect ./src/index.ts",
    "live": "NO_WEBSERVER=true nodemon --exec npm run start:debug --watch ./src",
    "prebuild": "npm run clean",
    "build": "tsc -p . --pretty",
    "copy:dotenv": "cp-cli .env ./release/.env",
    "clean": "rimraf release/*",
    "clean:dotenv": "rimraf ./release/.env"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.0.39",
    "@types/node": "^8.0.53",
    "nodemon": "^1.12.1",
    "rimraf": "^2.6.2",
    "ts-node": "^3.3.0",
    "typescript": "^2.6.2"
  },
  "dependencies": {
    "botgram": "^2.0.1",
    "dotenv": "^4.0.0",
    "express": "^4.16.2",
    "morgan": "^1.9.0"
  }
}
